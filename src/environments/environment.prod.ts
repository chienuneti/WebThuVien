/**
 * Environment configuration for production
 */

export const environment = {
  production: true,
  useMockBackend: false, // Always use real API in production
  apiUrl: 'https://api.example.com/api/auth', // Update with real API
};
